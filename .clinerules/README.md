# .clinerules

This folder contains repository-specific instructions for AI-assisted edits.

Files are split by concern so future updates can stay focused:
- `project.md` for repository architecture and assumptions
- `python.md` for Python implementation rules
- `shell.md` for Bash scripting rules
- `configuration.md` for YAML and Brewfile editing guidance
- `safety.md` for change-management and platform safety constraints