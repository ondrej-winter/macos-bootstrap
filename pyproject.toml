[project]
name = "macos-bootstrap"
version = "1.0.0"
description = "Automated macOS bootstrap with Homebrew and system configuration"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0",
]

[dependency-groups]
dev = []
